# react-spfx-app
React/SharePoint Framework starter app.

This is  web part that was created using the `yo @microsoft/sharepoint` to scaffold a sharepoint project. It's the basic banner web part with a few updates to the text, background, button, and properties. Take the following steps to use this start app:

1. Clone this folder and run `npm install`
2. 
